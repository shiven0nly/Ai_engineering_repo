# Lists
"""
Python lists are containers to store a set of values of any data type

A list can be indexed just like a string

L1 = [7 , "Ashiv" , 34.22 , False]

"""

# ============
# LIST METHODS:
"""
1. L1.sort() : sorting in acending order
2. L1.reverse() : reverse the list
3. L1.append(entity) : adds the entity in the end of list
4. L1.insert(index, entity) : this will add the insert the entity at given index, it wont remove the entity present their already, but it insert it there
5. L1.pop(index) : it will remove the index passed in pop function
6. L1.remove(entity) : will remove entityt from the list
7. L1[index1 : index2] : for slicing
"""

# =========
# TUPLES
"""
Tuples are lists that are not mutable , they are like strings.. but they can store data like lists

We made tuples by using simple brackets 

a = (1) # it encounter as an integer
a = (1,) # it encounter as an tuple

METHODS
1. a.count(entity) : it will count the entity present in the tuple
2. a.index(entity) : it will returns the index of the first occurence of a specified value in the tuple. Raises a 'ValueError' if the value is not found

"""



